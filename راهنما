بله، حتما. متن زیر دقیقاً با فرمت **Markdown** آماده شده است.

شما می‌توانید کل باکس زیر را کپی کنید و در گیت‌هاب یک فایل جدید (مثلاً به نام `GUIDE-FA.md` یا `README.md`) بسازید و این متن را داخل آن پیست کنید. در گیت‌هاب دقیقاً به صورت یک راهنمای تمیز و مرتب نمایش داده می‌شود.

```markdown
# 🌐 راهنمای ساده و کامل نصب تونل DNS (برای مبتدیان)

این راهنما مخصوص کسانی نوشته شده است که دانش فنی کامپیوتری ندارند و می‌خواهند روی ویندوز، به ساده‌ترین روش ممکن یک تونل DNS برای دسترسی به اینترنت آزاد بسازند.

---

## 🛠 پیش‌نیازها (مواد لازم)
قبل از شروع، باید این ۳ مورد را داشته باشید:
1. **یک سرور مجازی (VPS):** باید خریده باشید و **آی‌پی (IP)** و **رمز عبور (Password)** آن را داشته باشید.
2. **یک دامنه:** یک آدرس اینترنتی شخصی (مثل `mysite.com`) که به کلادفلر متصل باشد.
3. **کامپیوتر ویندوزی:** برای اجرای برنامه نصب‌کننده.

---

## قدم ۱: تنظیم آدرس‌ها در کلادفلر (بسیار مهم)
وارد پنل **Cloudflare** شوید، دامنه خود را انتخاب کنید و به بخش **DNS** بروید. باید دقیقاً **دو رکورد** جدید بسازید:

### ۱. رکورد اول (معرفی سرور)
* **Type:** را روی `A` بگذارید.
* **Name:** بنویسید `ns1`
* **IPv4 address:** آدرس عددی سرور خود را وارد کنید (مثلاً `1.2.3.4`).
* **Proxy status:** حتماً **خاموش** (رنگ خاکستری / DNS Only) باشد.
* دکمه **Save** را بزنید.

### ۲. رکورد دوم (ساخت تونل)
* **Type:** را روی `NS` بگذارید.
* **Name:** بنویسید `t`
* **Nameserver:** آدرس رکورد قبلی را بنویسید. یعنی: `ns1.dominshoma.com`
    * *(به جای `dominshoma.com` آدرس دامنه خودتان را بنویسید)*.
* دکمه **Save** را بزنید.

> **نتیجه:** آدرس تونل شما می‌شود: `t.dominshoma.com` (این را جایی یادداشت کنید).

---

## قدم ۲: آماده‌سازی ویندوز
روی دکمه **Start** ویندوز راست‌کلیک کنید و گزینه **Terminal (Admin)** یا **PowerShell (Admin)** را انتخاب کنید.
سپس دستور زیر را کپی کرده و در محیط آبی/مشکی پیست کنید و اینتر بزنید تا ابزارهای لازم نصب شوند:

```powershell
winget install -e --id Git.Git; winget install -e --id Python.Python.3

```

*(نکته: اگر سوالی پرسید `Y` بزنید. بعد از پایان نصب، حتماً پنجره ترمینال را ببندید و دوباره باز کنید).*

---

## قدم ۳: دانلود و اجرای برنامه

دستورات زیر را به ترتیب (خط به خط) کپی کنید و در پاورشل (PowerShell) اجرا کنید:

**۱. دانلود برنامه:**

```powershell
git clone [https://github.com/therealaleph/dns-tunnel-deploy.git](https://github.com/therealaleph/dns-tunnel-deploy.git)

```

**۲. ورود به پوشه:**

```powershell
cd dns-tunnel-deploy\gui

```

**۳. ساخت محیط اجرایی:**

```powershell
python -m venv venv

```

**۴. فعال‌سازی:**

```powershell
.\venv\Scripts\Activate

```

**۵. نصب پیش‌نیازها:**

```powershell
pip install paramiko pyinstaller

```

**۶. اجرای برنامه:**

```powershell
python dnstt_manager.py

```

---

## قدم ۴: تنظیمات داخل برنامه

بعد از دستور آخر، یک پنجره باز می‌شود. اطلاعات را به صورت زیر وارد کنید:

1. **بخش SSH Connection (بالا):**
* **Server IP:** آی‌پی سرور خود را وارد کنید.
* **Username:** بنویسید `root`
* **Password:** رمز عبور سرور را وارد کنید.
* روی دکمه **Connect** کلیک کنید و صبر کنید تا وصل شود.


2. **بخش DNSTT Configuration (پایین):**
* **NS Domain:** آدرسی که در قدم اول ساختید را وارد کنید (مثلاً `t.dominshoma.com`).
* روی دکمه **Deploy DNSTT** کلیک کنید.



⏳ **صبر کنید!** برنامه حدود ۱ تا ۲ دقیقه کار می‌کند. در پایان، یک لینک طولانی که با `dns://` شروع می‌شود به شما می‌دهد. دکمه **Copy URI** را بزنید.

---

## قدم ۵: اتصال در گوشی یا کامپیوتر

لینک کپی شده را در برنامه‌های زیر استفاده کنید:

* **اندروید:** برنامه **V2RayNG** (لینک را Import کنید).
* **ویندوز:** برنامه **v2rayN** یا **Nekoray**.
* **آیفون:** برنامه **V2Box** یا **Streisand**.

✅ **تمام شد! شما اکنون به اینترنت آزاد دسترسی دارید.**

```

```
